{
  "name": "farmerchain",
  "version": "1.0.0",
  "description": "Decentralized Agricultural Marketplace - Connecting farmers directly with buyers through blockchain technology",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run start",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["blockchain", "agriculture", "marketplace", "ethereum", "hedera", "farmers", "decentralized"],
  "author": "FarmerChain Team",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.12.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
